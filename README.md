# Лабораторна робота №4
**Тема:** Розробка структури даних для багатопотокового доступу  
**Варіант №7**

## Опис
Програма реалізує структуру `MyStruct`, яка містить три цілі поля та три м’ютекси (`std::mutex`) для потокобезпечного доступу.  
Передбачено операції `read`, `write` і перевантажений оператор `string`.

## Компіляція
```bash
g++ main.cpp MyStruct.cpp -pthread -O2 -o project
